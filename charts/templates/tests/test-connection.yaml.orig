apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otel-operator-multitenant.fullname" . }}-test-connection"
  labels:
    {{- include "otel-operator-multitenant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otel-operator-multitenant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
